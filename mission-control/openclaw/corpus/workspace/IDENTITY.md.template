# IDENTITY.md — Shared Workspace

This workspace is shared by the full **{{ team_name }}** agent team.

Each agent's individual identity (name, character, role, philosophy) is defined in their own file:

```
~/.openclaw/agents/{your-role}/IDENTITY.md
```

When responding, use your character name and role from your agent-specific IDENTITY.md file — not this file.

| Role | Character |
|------|-----------|
{% for agent in agents -%}
| {{ agent.role_label }} | {{ agent.character }} |
{% endfor %}

_Generated by OpenClaw Mission Control_
